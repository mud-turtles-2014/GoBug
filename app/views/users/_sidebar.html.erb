<ul id="sidebar_menu" class="sidebar-nav">
  <li class="sidebar-brand"><a id="menu-toggle" href="#">Hello, <%=@user.username%><span id="main_icon" class="glyphicon glyphicon-align-justify"></span></a></li>
</ul>
<h5>Your Trips</h5>
  <ul class="sidebar-nav" id="sidebar">
		<% @trips.each do |trip| %>
		  <li><%= link_to trip.name, fetch_expenses_path(:trip_id => trip), :remote => true %></li>
		<% end %>
    <li><%= link_to "Add a Trip +", fetch_new_trip_path, :remote => true %></li>
  </ul>

<h5>Your WishLists</h5>
   <ul class="sidebar-nav" id="sidebar">
		  <% @wishlists.each do |wishlist| %>
		    <li><%= link_to wishlist.name, fetch_wishlist_items_path(:wishlist_id => wishlist), :remote => true %></li>
		  <% end %>
    <li><%= link_to "Add a Wishlist +", fetch_new_wishlist_path, :remote => true %></li>
  </ul>